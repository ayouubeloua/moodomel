# -*- coding: utf-8 -*-
"""
Created on Thu Apr 27 11:00:00 2017

@author: hp
"""

import string 
def module_booléen1(x):
    M=liste_desmots_du_documents
    n=liste_des_docs
    x=input("saisir la requete:")
    x.split()
    for i in range len(x):
        if x[i]==" " or x[i]="AND"
            for j in range len[M]:
                for k in range len[M]:
                    if x[i-1]==M[j] and x[i+1]=M[k]:
                        return (n)
                    exse: return ("aucun document repond à la requete")
                    
        elif x[i]== "or" or L[i]=="OR" 
              for j in range len[M]:
                for k in range len[M]:
                    if L[i-1]==M[j] and L[i+1]=M[k]:
                        return (N)
                   else: return ("aucun document repond à la requete")  
        elif l[i]=="SAUF" or l[i]=="-"           
                for j in range len[M]:
                    for k in range len[M]:
                        if L[i-1]==M[j] and L[i+1]=M[k]:
                            return (N)
                    else: return ("aucun document repond à la requete")                                    